<UserControl x:Class="Harmonics.Views.MenuPages.ControlPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <StackPanel>
        <StackPanel Name="TopPanel" Background="#242F3D" Orientation="Horizontal" Height="50" VerticalAlignment="Top">
            <Button Padding="0" Click="ButtonPrev_OnClick"
                    Background="{x:Null}" BorderBrush="{x:Null}" Width="25" >
                <materialDesign:PackIcon Kind="ArrowBack" Foreground="Gray"/>
            </Button>
            <Button Padding="0" Click="ButtonNext_OnClick"
                    Background="{x:Null}" BorderBrush="{x:Null}" Width="25" >
                <materialDesign:PackIcon Kind="ArrowForward" Foreground="Gray"/>
            </Button>
        </StackPanel>
        <Grid Name="ContentGrid" Background="Aquamarine" Height="350" >
        </Grid>
        
    </StackPanel>
</UserControl>
