namespace Harmonics.Models.ServerProvider
{
    public enum MessageType
    {
        LogInMessage,
        RegisterMessage,
        ChatMessage,
        DeleteUser,
        CreateChat,
        SaveSettings
    }
}